let tailwindScreens; // Define it outside the event listener

document.addEventListener("DOMContentLoaded", (event) => {
    tailwindScreens = {
        mob: "640px",
        tab: "768px",
        desk: "1024px",
    };

    console.log(tailwindScreens);
});

// Now you can
